stages:
- name: compile
  steps:
  - publishImageConfig:
      dockerfilePath: ./PlaylistWorker/Dockerfile
      buildContext: .
      tag: playlist-worker
      pushRemote: true
      registry: index.docker.io
timeout: 60
notification: {}
